#没有登陆。
NotLogin:
  #优先执行Allow
  Allow:
    #支持正则匹配
    - "l(ogin)?"
    - "reg(ister)?"
    - "recoverpassword"
    - "rp"
    - "changepassword"
    - "cp"
    - "mailcode"
    - "mailogin"
  #禁用 全部(all)
  Disallow: all
BeLogin:
  Allow:
    - ""
  Disallow:
    - "pl(ugins)?"
    - "bukkit"
    - "spigot"